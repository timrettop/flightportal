config = {
    'bounds_box':         'your bounds', # N,S,W,E  - for fr24 api
    'home_airport':       'ZRH',
    'filter_direction':   False,
    'show_arrivals':      True,
    'show_departures':    True,
    'arrival_heading':    270,  # compass heading planes fly on approach over you; departures assumed reciprocal
    'heading_tolerance':  50,   # +/- degrees around that course for heading-based classification
    'heading_min':        240,  # only applicable if filter_direction is True
    'heading_max':        300,  # only applicable if filter_direction is True
    'min_altitude':       0,     # ft; ignore traffic below this
    'max_altitude':       7000,  # ft; ignore traffic above this
    'temp_unit':          'F',  # 'F' or 'C'
    'timezone':           'UTC', # e.g. 'Europe/Zurich'
    'show_full_aircraft': False,
    'show_helicopters':   False,

    # Feature flags - set to False to disable
    'enable_flights':     True,
    'enable_weather':     True,
    'enable_clock':       True,  # full-screen clock shown before the weather screen

    'demo_mode':          False,  # True = scripted demo feed (needs demo_feed.py on the board)

    # Screen sleep - blanks the display overnight (or whenever) to save power / avoid glare.
    # Current time is fetched from Adafruit IO's free time service (no account needed), which
    # returns UTC. 'sleep_utc_offset' is a manual, non-DST-aware fallback (in hours) - normally
    # a DST-aware offset is learned automatically from Open-Meteo (via weather, or the clock's
    # own periodic sync if weather is disabled), so this only matters if both are off.
    'sleep_enabled':     False,
    'sleep_start':       '23:00',  # HH:MM local time to blank the display
    'sleep_end':         '06:00',  # HH:MM local time to wake it back up
    'sleep_utc_offset':  0,        # hours to add to UTC to get local time, e.g. -5, 1, 5.5
}
