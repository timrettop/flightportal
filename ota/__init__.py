# ota -- signed over-the-air updates for CircuitPython.
#
# Trusted core (USB only): verify.py, pubkey.py, state.py, recovery.py
# Everything else in this package ships over the air — see ota-files.txt for what's current.